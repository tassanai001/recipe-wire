// Placeholder export for @recipe-wire/utils
// This package will contain shared utility functions

// Example utility function - will be expanded with more utilities in later stories
export const formatDate = (date: Date): string => {
  return new Intl.DateTimeFormat('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  }).format(date)
}

export const truncateText = (text: string, maxLength: number): string => {
  if (text.length <= maxLength) {
    return text
  }
  return text.substring(0, maxLength) + '...'
}

// Additional utility functions will be added in later stories based on requirements
